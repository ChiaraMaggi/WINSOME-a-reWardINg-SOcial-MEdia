CLIENT MAIN:
implementato parsing del file di configurazione o nel caso settaggio di default.
implementato una gestione degli errori di notazione di tutti i possibili comandi che l'utente può digitare.
implememtata connessione tcp con server

SERVER MAIN:
implementato pasring del file di configurazione o nel caso settaggio di default.
implementata connessione tcp con clients

SERVER RUNNABLE:
implementata capacità di ricevere dal client le richieste

SOMMARIO:
implementato register, login e logout quasi sicuamente corretti

TO DO:
implemntare la gestione delle richieste lato SERVER RUNNABLE
thread che si occupa di chiudere le connessioni

SCELTE:
scelto threadpool per gestire richieste, molo elastico.
scelto trasferimento dati via stream